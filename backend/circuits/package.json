{
  "//": [
    "bb 6.0.0-nightly IS THE TOOLCHAIN, and the only one. Resolved as ./node_modules/.bin/bb.",
    "",
    "5.1.0 is gone from this tree: no script, tool, image or fixture references it, and every",
    "committed proof and verifier was regenerated on 6.0 (TODO sec. 2.18eh). If it is still",
    "installed at ~/.bb it is now inert - nothing here will pick it up.",
    "",
    "IT HAD TO BE 6.0 REGARDLESS: 5.1.0 builds the chonk fold's app and kernels but REJECTS the",
    "wrapper's HN recursion constraints outright, so the fold path does not exist without it.",
    "",
    "THE CONTAINER DOES NOT SHIP bb EITHER. passport-verifiers.Dockerfile pins only nargo; the",
    "runner symlinks this package's own linux binary from the mounted repo onto PATH. One pin, in",
    "one file, for host and container alike - and a bb bump is `npm install`, not an image rebuild.",
    "",
    "WHY THE PIN IS EXACT, AND WHY A VK CHECK WILL NOT PROTECT YOU. Measured, both directions, with",
    "the same-toolchain control run first:",
    "  - withdraw_identity's recursion VK is BYTE-IDENTICAL under 5.1.0 and 6.0. Same 115 fields,",
    "    same values. A pinned-VK check - which is exactly what inner_vk.nr is - sees nothing wrong.",
    "  - Its proofs are mutually unverifiable anyway. Each verifies against its own bb; each fails",
    "    against the other at `UltraVerifier: verification failed at reduction step`.",
    "  - The emitted Solidity moved 2,491 -> 2,518 lines, 99 lines differing, with ZERO VK constants",
    "    changed. So the TEMPLATE is what breaks a verifier, and the key cannot tell you.",
    "",
    "A NIGHTLY IS PINNED DELIBERATELY. There is no 6.0.0 release. Pinned to an exact date rather",
    "than a range because a nightly may change proof formats between builds, and that change is",
    "invisible to every guard in this repo. Bump on purpose, then regenerate BOTH on-chain fixtures",
    "(ChonkRootProofOnChain, RecursionTreeProofOnChain) and re-run codegen-verifiers.sh."
  ],
  "devDependencies": {
    "@aztec/bb.js": "6.0.0-nightly.20260804",
    "prettier": "^3.6.1"
  }
}
